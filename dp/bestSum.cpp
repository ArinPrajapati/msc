#include<bits/stdc++.h>
#include <cstddef>

using namespace std;


vector<int>* bestSum(int targetSum, vector<int> &numbers) {


    if(targetSum < 0) {
        return nullptr;
    };
    if(targetSum == 0) return {};

    auto solver = [targetSum,numbers](){

    }

}
